# Track only markdown files in this repo.
# Ignore everything by default...
*

# ...then re-allow:
!/.gitignore    # only the root .gitignore (not nested ones like .idea/.gitignore)
!*/             # descend into directories so .md files inside are found
!*.md           # any markdown file at any depth
